import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class MakeYourOwnPlanScreen extends StatelessWidget {
  const MakeYourOwnPlanScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.only(
        top: 32.h,
        left: 32.h,
      ),
      child: ListView(
        children: [],
      ),
    );
  }
}
